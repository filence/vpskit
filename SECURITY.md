# 安全策略

## 报告漏洞

在项目尚未建立公开安全邮箱前，请通过私有渠道向维护者报告，不要在公开 Issue 中提交节点链接、VPS 地址、域名、API Token、EAB、私钥、Cookie 或 SSH 凭据。

报告应包含受影响版本、最小复现步骤、影响和已做的脱敏处理。维护者确认前不要公开可直接利用的细节。

## 凭据边界

- 生产 Token 不进入源码、CI、发布包、备份、命令行参数、日志或审计详情。
- 本地凭据文件由 `.gitignore` 排除，只用于授权的实验 VPS。
- CI 只使用公开上游资产，不允许读取生产 Cloudflare/ACME 凭据。
- 发现疑似泄露时，应先吊销或轮换对应凭据，再清理历史；仅删除当前文件不足以消除泄露。
